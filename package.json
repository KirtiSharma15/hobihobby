{
  "name": "hobihobby",
  "version": "1.0.0",
  "description": "HobiHobby - AI-powered cross-platform hobby discovery app",
  "private": true,
  "scripts": {
    "dev": "npm run web:dev",
    "start": "npm run web:start",
    "build": "npm run web:build",
    "install:all": "npm install && npm run web:install",
    "lint": "npm run web:lint",
    "lint:fix": "npm run web:lint:fix",
    "test": "npm run web:test",
    "clean": "npm run web:clean",
    "web:dev": "cd web && npm run dev",
    "web:start": "cd web && npm start",
    "web:build": "cd web && npm run build",
    "web:install": "cd web && npm install",
    "web:lint": "cd web && npm run lint",
    "web:lint:fix": "cd web && npm run lint:fix",
    "web:test": "cd web && npm test",
    "web:clean": "cd web && npm run clean"
  },
  "keywords": [
    "react",
    "typescript",
    "firebase",
    "hobby",
    "ai"
  ],
  "author": "HobiHobby Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0"
  }
}
